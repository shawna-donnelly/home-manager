# Plug'n'Play (Yarn's default) breaks editor tsservers and anything that
# resolves from node_modules. Nothing here benefits from PnP, so use the
# standard layout. Applies to both server/ and web/.
nodeLinker: node-modules
